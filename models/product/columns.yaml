# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    name:
        label: awebsome.products::lang.label.name
    gallery:
        label: awebsome.products::lang.label.gallery
        relation: gallery
        select: concat(name,' ',description )
